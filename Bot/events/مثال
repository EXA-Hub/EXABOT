const { Client } = require('discord.js');

/**
 * @param {Client} client
 */

module.exports = (client, instance) => {}

module.exports.config = {
    displayName: '',
    dbName: '' // This should NEVER be changed once set, and users cannot see it.
}